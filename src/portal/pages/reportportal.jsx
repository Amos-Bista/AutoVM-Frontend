import { Box } from "@mui/material";
import React from "react";

const Reportportal = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        color: "white",
        backgroundColor: "black",
        height: "100%",
        padding: "3rem",
      }}
    >
      <h1>This is the Report portal component.</h1>
    </Box>
  );
};

export default Reportportal;
